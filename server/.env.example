# Copy to server/.env for local dev. In production set these as real
# environment variables on the host — never commit the filled-in file.
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>/folio?retryWrites=true&w=majority
PORT=3000
