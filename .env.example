# Copy to .env for local development.
# Vite reads VITE_* variables at BUILD time and compiles the value into the
# JavaScript bundle, so never put a secret here — it ships to every visitor.
# In production this is supplied as a Docker build argument instead
# (Dokploy: service -> Build Time Arguments).
VITE_API_BASE_URL=http://localhost:3000
